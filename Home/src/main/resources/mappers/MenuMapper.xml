<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ohgiraffers.home.menu.model.dao.MenuMapper">
    <select id="findMenuList" resultType="com.ohgiraffers.home.menu.model.dto.MenuDTO">
        SELECT * FROM TBL_MENU
    </select>
    <select id="findCategoryList" resultType="com.ohgiraffers.home.menu.model.dto.CategoryDTO">
        SELECT * FROM TBL_CATEGORY
    </select>
    <insert id="registMenu">
        INSERT INTO TBL_MENU(MENU_NAME, MENU_PRICE, CATEGORY_CODE, ORDERABLE_STATUS)
        VALUES (#{menuName}, #{menuPrice}, #{categoryCode}, #{orderableStatus})
    </insert>
</mapper>